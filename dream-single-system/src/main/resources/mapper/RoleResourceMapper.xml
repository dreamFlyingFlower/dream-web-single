<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dream.system.mapper.RoleResourceMapper">

	<resultMap id="BaseResultMap" type="com.dream.system.entity.RoleResourceEntity">
		<id column="id" jdbcType="BIGINT" property="id" />
		<result column="role_id" jdbcType="BIGINT" property="roleId" />
		<result column="resource_id" jdbcType="BIGINT" property="resourceId" />
		<result column="resource_type" jdbcType="TINYINT" property="resourceType" />
		<result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
		<result column="create_user" jdbcType="BIGINT" property="createUser" />
		<result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
		<result column="update_user" jdbcType="BIGINT" property="updateUser" />
	</resultMap>

	<sql id="BaseColumnList">
		a.id, a.role_id, a.resource_id, a.resource_type, a.create_time, a.create_user, a.update_time, a.update_user
	</sql>

	<select id="list" resultMap="BaseResultMap">
		SELECT
		<include refid="BaseColumnList" />
		FROM ts_role_resource a
		<where>
			<if test="query.id != null and query.id != ''">
				AND a.id = #{query.id, jdbcType=BIGINT }
			</if>
			<if test="query.roleId != null and query.roleId != ''">
				AND a.role_id = #{query.roleId, jdbcType=BIGINT }
			</if>
			<if test="query.resourceId != null and query.resourceId != ''">
				AND a.resource_id = #{query.resourceId, jdbcType=BIGINT }
			</if>
			<if test="query.resourceType != null and query.resourceType != ''">
				AND a.resource_type = #{query.resourceType, jdbcType=TINYINT }
			</if>
		</where>
		ORDER BY a.id DESC
	</select>
</mapper>