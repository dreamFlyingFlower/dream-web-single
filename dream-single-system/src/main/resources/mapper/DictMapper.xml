<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dream.system.mapper.DictMapper">

	<resultMap id="BaseResultMap" type="com.dream.system.entity.DictEntity">
		<id column="id" jdbcType="BIGINT" property="id" />
		<result column="dict_code" jdbcType="VARCHAR" property="dictCode" />
		<result column="dict_name" jdbcType="VARCHAR" property="dictName" />
		<result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
		<result column="create_user" jdbcType="BIGINT" property="createUser" />
		<result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
		<result column="update_user" jdbcType="BIGINT" property="updateUser" />
	</resultMap>

	<sql id="BaseColumnList">
		a.id, a.dict_code, a.dict_name, a.create_time, a.create_user, a.update_time, a.update_user
	</sql>

	<select id="list" resultMap="BaseResultMap">
		SELECT
		<include refid="BaseColumnList" />
		FROM sys_dict a
		<where>
			<if test="query.id != null and query.id != ''">
				AND a.id = #{query.id, jdbcType=BIGINT }
			</if>
			<if test="query.dictCode != null and query.dictCode != ''">
				AND a.dict_code = #{query.dictCode, jdbcType=VARCHAR }
			</if>
			<if test="query.dictName != null and query.dictName != ''">
				AND a.dict_name = #{query.dictName, jdbcType=VARCHAR }
			</if>
		</where>
		ORDER BY a.id DESC
	</select>
</mapper>