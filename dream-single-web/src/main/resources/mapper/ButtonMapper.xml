<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dream.web.mapper.ButtonMapper">

	<resultMap id="BaseResultMap" type="com.dream.web.entity.Button">
		<id column="id" jdbcType="BIGINT" property="id" />
		<result column="button_name" jdbcType="VARCHAR" property="buttonName" />
		<result column="menu_id" jdbcType="BIGINT" property="menuId" />
		<result column="url" jdbcType="VARCHAR" property="url" />
		<result column="icon" jdbcType="VARCHAR" property="icon" />
		<result column="sort" jdbcType="INTEGER" property="sort" />
		<result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
		<result column="create_user" jdbcType="BIGINT" property="createUser" />
		<result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
		<result column="update_user" jdbcType="BIGINT" property="updateUser" />
	</resultMap>

	<sql id="BaseColumnList">
		a.id, a.button_name, a.menu_id, a.url, a.icon, a.sort, a.create_time, a.create_user,
		a.update_time, a.update_user
	</sql>

	<select id="list" resultMap="BaseResultMap">
		SELECT
		<include refid="BaseColumnList" />
		FROM ts_button a
		<where>
			<if test="query.id != null and query.id != ''">
				AND a.id = #{query.id, jdbcType=BIGINT }
			</if>
			<if test="query.buttonName != null and query.buttonName != ''">
				AND a.button_name = #{query.buttonName, jdbcType=VARCHAR }
			</if>
			<if test="query.menuId != null and query.menuId != ''">
				AND a.menu_id = #{query.menuId, jdbcType=BIGINT }
			</if>
			<if test="query.url != null and query.url != ''">
				AND a.url = #{query.url, jdbcType=VARCHAR }
			</if>
			<if test="query.icon != null and query.icon != ''">
				AND a.icon = #{query.icon, jdbcType=VARCHAR }
			</if>
			<if test="query.sort != null and query.sort != ''">
				AND a.sort = #{query.sort, jdbcType=INTEGER }
			</if>
		</where>
		ORDER BY a.id DESC
	</select>
</mapper>