<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.electric.system.mapper.SysUserMapper">

	<select id="getList" resultType="com.electric.system.entity.SysUserEntity">
		select t1.*
			from sys_user t1 where t1.super_admin = 0 and t1.deleted = 0
		<if test="username != null and username.trim() != ''">
			and t1.username like concat('%', #{username}, '%')
		</if>
		<if test="mobile != null and mobile.trim() != ''">
			and t1.mobile = #{mobile}
		</if>
		<if test="gender != null">
			and t1.gender = #{gender}
		</if>
		<if test="status != null">
			and t1.status = #{status}
		</if>
		<if test="orgId != null">
			and t1.org_id =#{orgId}
		</if>
		<if test="realName != null and realName.trim() != ''">
			and t1.real_name like concat('%', #{realName}, '%')
		</if>
		<if test="userCert != null">
			<choose>
			    <when test="userCert">
			       and t1.user_cert IS NOT NULL
			    </when>
			    <otherwise>
			        and t1.user_cert IS  NULL
			    </otherwise>
			</choose>
		</if>
	</select>

	<select id="getById" resultType="com.electric.system.entity.SysUserEntity">
		select t1.*, (select t2.name from sys_org t2 where t2.id = t1.org_id) orgName from sys_user t1
			where t1.id = #{id} and t1.deleted = 0
	</select>

	<select id="getRoleUserList" resultType="com.electric.system.entity.SysUserEntity">
		select t1.* from sys_user t1 left join sys_user_role t2 on t1.id = t2.user_id
			where t1.deleted = 0 and t2.deleted = 0 and t2.role_id = #{roleId}
		<if test="username != null and username.trim() != ''">
			and t1.username = #{username}
		</if>
		<if test="mobile != null and mobile.trim() != ''">
			and t1.mobile = #{mobile}
		</if>
		<if test="gender != null">
			and t1.gender = #{gender}
		</if>
	</select>
	
	
	<update id="updateUserCert">
	   update `sys_user` set  `user_cert` = NULL  where `id` =#{id};
	</update>

</mapper>