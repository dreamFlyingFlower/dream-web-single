<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dream.web.mapper.PermissionMapper">

	<resultMap id="BaseResultMap" type="com.dream.web.entity.Permission">
		<id column="id" jdbcType="BIGINT" property="id" />
		<result column="permission_code" jdbcType="VARCHAR" property="permissionCode" />
		<result column="permission_name" jdbcType="VARCHAR" property="permissionName" />
		<result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
		<result column="create_user" jdbcType="BIGINT" property="createUser" />
		<result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
		<result column="update_user" jdbcType="BIGINT" property="updateUser" />
	</resultMap>

	<sql id="BaseColumnList">
		a.id, a.permission_code, a.permission_name, a.create_time, a.create_user, a.update_time, a.update_user
	</sql>

	<select id="list" resultMap="BaseResultMap">
		SELECT
		<include refid="BaseColumnList" />
		FROM ts_permission a
		<where>
			<if test="query.id != null and query.id != ''">
				AND a.id = #{query.id, jdbcType=BIGINT }
			</if>
			<if test="query.permissionCode != null and query.permissionCode != ''">
				AND a.permission_code = #{query.permissionCode, jdbcType=VARCHAR }
			</if>
			<if test="query.permissionName != null and query.permissionName != ''">
				AND a.permission_name = #{query.permissionName, jdbcType=VARCHAR }
			</if>
		</where>
		ORDER BY a.id DESC
	</select>
</mapper>